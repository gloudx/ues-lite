id: com.example.user
version: 1.0.0
name: User Profile
description: Basic user profile schema with required and optional fields
status: active
schema: |
  type User struct {
    username String
    email String
    age Int
    active Bool
    bio optional String
    tags optional [String]
  }